# 拼豆工作室 1.2

手機優先 PWA。新增本機 AI 圖紙辨識流程：圖片裁切、格數建議、每格色彩採樣、MARD 221 色最近色匹配、逐格修正，再套用到圖紙編輯器。

注意：這是純前端版本，辨識在手機瀏覽器本機執行，不是雲端大型 AI 模型；照片傾斜、陰影、網格線、透明豆等情況可能造成誤判，因此保留逐格人工修正。

GitHub Pages 可直接部署靜態檔案；推送到 main 後會自動發布。
